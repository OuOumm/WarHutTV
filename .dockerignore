frontend/node_modules
backend/dist
bin
.git
**/.husky/_
# NOTE: do NOT ignore frontend/dist here. The release workflow's docker job
# pre-builds the frontend locally and copies the artifact into a scratch image
# via Dockerfile.release (COPY frontend/dist). Excluding it would make that
# COPY fail silently. The main multi-stage Dockerfile rebuilds dist anyway.
