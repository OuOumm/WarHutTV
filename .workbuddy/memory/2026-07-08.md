# 2026-07-08

## 前端架构与四维分析报告（Frontend 专家）
- 产出：`frontend/ANALYSIS_REPORT.md`（基于 `frontend/src` 实际代码，逐文件:行号定位）。
- 结论概要：
  - 性能：播放器已 lazy 分包（好）；**缺路由级代码分割**（App.tsx 全静态 import）、长列表无虚拟化（VideoGrid/LazyGrid）、memo 被内联 onDelete 击穿（Home.tsx:213）。
  - 动效：**全屏背景常驻重绘**是最大性能隐患（index.css body::before/::after + orbs；texNoise 0.3s 最重）；`prefers-reduced-motion` 覆盖不全（Login 内联 keyframes、scan-line/radar 未关）。
  - 视觉：体系统一（好）；次要文字 `muted/40~/60` 对比度不足（AA 4.5:1 未达）；6 主题各自字体/行高差异致错位。
  - 可访问性（最大缺口）：**无全局 `:focus-visible`**、模态无 dialog/焦点陷阱/Esc、表单无 label、Toast 无 live region、Home 无 h1、无 skip-link。
- 优先级：P0 焦点环+无障碍语义 → P1 路由 lazy+背景动效降级+reduced-motion+对比度 → P2 虚拟化+memo+语义补全。

## 修复：移动端卡片播放按钮遮挡标题
- 文件：`frontend/src/components/VideoCard.tsx`
- 问题：桌面端 hover 用的 `PlayOverlay` 播放按钮在移动端作为覆盖层存在（`z-[3]`），卡片标题容器仅 `z-[1]`，窄屏交互时可能压住标题。
- 修复：
  1. `PlayOverlay` 容器改为 `hidden sm:flex` —— 移动端 (<640px) `display:none` 彻底不渲染；桌面端 hover 行为不变。
  2. 标题容器（Bottom info）层级由 `z-[1]` 提到 `z-[5]`，为卡片内最高，确保标题永不被任何元素覆盖。
- 验证：`npm run build` 通过；built CSS 确认 `.hidden{display:none}`、`@media(width>=40rem){.sm\:flex{display:flex}}`、`z-index:5` 均存在。
- 说明：PlayOverlay 仅为桌面 hover 装饰，卡片本身是 `<Link>` 点击即跳转播放页，移动端移除播放按钮不影响功能。

## 制定代码审查标准与流程（Code Review 体系）
- 产出 3 个文件：
  1. `docs/code-review-guide.md` —— 总标准与流程（角色/PR生命周期/CI门禁/合并门槛/优先级 P0-P3/Go与TS专项/安全专项/注释规范/4阶段落地路线）。
  2. `.github/PULL_REQUEST_TEMPLATE.md` —— PR 提交模板（类型/自测/审查重点/作者自检/影响范围）。
  3. `docs/code-review-checklist.md` —— 审查者一页纸速查卡。
- 设计原则：对齐现有 `docs/code-review-*.md` 的 P0-P4 约定（P4 映射为 P3），并把 `docs/fix-plan.md` 的已知风险（默认弱密码、JWT 进 URL、SSRF、并发 map、假成功返回、响应体不限大小）写进专项标准。
- 关键现状（用于后续推行）：CI 仅构建无 lint/vet/test 门禁；前端 lint 现有 54 问题；后端因 embed 干净 checkout 跑不了 `go test`。落地路线建议阶段式：先流程+人工审查 → 加 lint/vet（新代码准入）→ 解 embed 后加 test → 覆盖率门槛。

## 代码审查 + 实际修复（按专家准则，不照搬 fix-plan）
- 用户要求：不要按 fix-plan.md，按代码审查专家自身准则独立审查并修复所有问题。
- 关键发现：`fix-plan.md` / `code-review-*.md` 均**已过时**——其上 P0（JWT 默认密钥、SSE token 进 URL、m3u8 代理缺失）与 `any[]`/`as any`/组件未提取等问题在**当前代码已修复**（JWT 启动自动生成；SSE 走 Authorization 头+fetch 流；`getPlayableUrl` 直连 m3u8 客户端过滤广告；Search.tsx 已有 SiteSearchResult 接口与提取组件）。
- 实际修复（后端，全部验证通过）：
  1. 🔴 P0 `config.go` `Snapshot()` 拷贝含 RWMutex 的 Config 值 → `go vet` 报错 copied lock value；新增无锁 `ConfigSnapshot` 类型修复。
  2. 🟠 P1 `cache.go` 缓存目录权限 0644→0755（原 0644 无执行位导致磁盘缓存 WriteFile 静默失败）。
  3. 🟠 P1 `jwt.go` 校验未断言签名算法 → 拒绝非 HMAC（algorithm confusion 防护）。
  4. 🟠 P1 `auth.go` 密码比较改 `crypto/subtle.ConstantTimeCompare`（时序侧信道）。
  5. 🟠 P1 `config.go` `UpdateConfig` 新增 `validateAPISite`（仅允许 http/https 可解析地址）。
  6. 🟡 P2 `bangumi.go` 复用 `services.ReadLimited`，删除重复的 `readLimitedBody`。
  7. 测试支柱：新增 `utils/jwt_test.go`、`services/proxy_test.go`、`config/config_test.go`、`handlers/config_test.go`（此前零测试）。
- 验证：`go build/vet/test` 全过；`npm run build`(tsc -b+vite) 通过；`npm run lint` = 0 error / 22 warning（react-refresh + react-hooks 建议，非缺陷）。
- 残留（评估后未强行改，附理由）：token 存 localStorage（P1，无 dangerouslySetInnerHTML 故风险低，改内存会丢刷新登录态）；前端 22 lint warning（P2/P3，多为规则误报/ DX 提示）。
- 产出审查记录：`docs/code-review-2026-07-08.md`（P0-P3 格式，含修复位置与验证表）。
